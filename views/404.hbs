<!DOCTYPE html>
<html>
    <head>
        <title>Weather APP | {{ title }}</title>
        <link rel="stylesheet" href="/assets/css/styles.css">
    </head>
    <body>
        <div class="main-content">
            {{>header}}
            404 - {{ errorMsg }}
        </div>
        {{>footer}}
    </body>
</html>