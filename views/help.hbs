<!DOCTYPE html>
<html>
    <head>
        <title>Weather APP | {{ title }}</title>
        {{>headScripts}}
    </head>
    <body>
        {{>header}}
        <div class="main-content container">
            <div class="row">
                <div class="col s12">
                    <p class="flow-text">{{ text }}</p>
                </div>
            </div>
        </div>
        {{>footer}}
        {{>bodyScripts}}
    </body>
</html>